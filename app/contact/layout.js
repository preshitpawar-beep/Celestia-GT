export const metadata = {
  title: "Contact",
  description:
    "Request a quotation for cast, machined or fabricated components. Celestia GT responds to enquiries within two working days.",
  alternates: { canonical: "/contact" },
};

export default function ContactLayout({ children }) {
  return children;
}
